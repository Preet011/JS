/*
poulets = 2 pattes
vaches = 4 pattes
chevaux = 4 pattes
*/


/*
function addition (chicken, cow, horse) {
    let total = parseInt(chicken) + parseInt(cow)+ parseInt(horse); 
    
};

let q1 = prompt("How many chickens");
let q2 = prompt("How many cows");
let q3 = prompt("How many horses");

addition(q1, q2, q3);

function multiply() {
  
    let result = q1 * 2 + q2 * 4 + q3  * 4; 
    
console.log(result);

};
resultUser = addition() + multiply();
document.write(
    `<p style='color:red'>Number of animals ${addition()} + number of paws =  ${resultUser}  </p>`
    );
*/
// correction